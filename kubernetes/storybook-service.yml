apiVersion: v1
kind: Service
metadata:
  name: storybook
  labels:
    app: components-ui
spec:
  ports:
    - name: port-8080
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: components-ui